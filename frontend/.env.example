# Local frontend defaults
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000/ws

# Admin panel access (UUID). Backend is authoritative via OWNER_ID.
VITE_OWNER_ID=replace-with-owner-uuid
